---
pack_id: WP-304
pack_type: work-package
work_package: WP-304
capability: "PC-02"
artifact_dependencies:
  - ARCH-04
  - ARCH-05
  - ARCH-09
  - INT-05
contract_dependencies:
  - "EmergencySafetyRequest"
  - "EmergencySafetyResponse"
  - "EmergencyProtocolDTO"
upstream_dependencies:
  - WP-302
  - WP-202
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-304

## 1. Purpose

Xây restricted caution/uncertain safety profile sau deterministic emergency gate.

## 2. Required Context

- Layer/module: L3 / M3.4.
- Capability trace: Emergency Safety: pre-filter deterministic, protocol an toàn và Level-1 contact handoff. 
- Owner pool/size: AI-CORE / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-304 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/business-sequences.md` — đọc các phần canonical liên quan tới ARCH-04.
- `docs/artifacts/architecture/ai-capability-mapping.md` — đọc các phần canonical liên quan tới ARCH-05.
- `docs/artifacts/architecture/deployment-resilience.md` — đọc các phần canonical liên quan tới ARCH-09.
- `docs/artifacts/interface/ai-behavior-contracts.md` — đọc các phần canonical liên quan tới INT-05.

## 4. Required Contracts

- `EmergencySafetyRequest` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencySafetyResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencyProtocolDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-02 orchestration pipeline.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-302` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-202` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-02 orchestration pipeline.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Không chẩn đoán/điều trị.
- Level 1 chỉ hướng dẫn contact handoff.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-304.
