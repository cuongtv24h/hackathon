---
pack_id: WP-007
pack_type: work-package
work_package: WP-007
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - ARCH-02
  - ARCH-08
  - INT-04
contract_dependencies:
  - "EmergencyKeywordSetDTO"
  - "EmergencyProtocolDTO"
  - "AppointmentDTO"
  - "AnalyticsSummaryDTO"
upstream_dependencies:
  - WP-003
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-007

## 1. Purpose

Kiểm kê `docs/knowledge`, đăng ký nguồn RAG và ánh xạ canonical seed entity sang schema.

## 2. Required Context

- Layer/module: L0 / M0.1.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: DATA / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-007 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/domain-model.md` — đọc các phần canonical liên quan tới ARCH-02.
- `docs/artifacts/architecture/integration-data-flow.md` — đọc các phần canonical liên quan tới ARCH-08.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.

## 4. Required Contracts

- `EmergencyKeywordSetDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencyProtocolDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AnalyticsSummaryDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

### Required Raw Inputs

- `docs/knowledge/Bang_gia_dich_vu_ky_thuat.md`
- `docs/knowledge/quy-trinh-don-tiep-benh-nhan_chuan-hoa-doi-chieu-nguon.md`
- `docs/knowledge/bhyt/quy-trinh-kham-bhyt.md`
- `docs/knowledge/bhyt/ho-so-giay-to-bhyt.md`
- `docs/knowledge/bhyt/pham-vi-quyen-loi-bhyt.md`
- `docs/knowledge/bhyt/tinh-huong-dac-biet-bhyt.md`
- `docs/knowledge/bhyt/danh-muc-dich-vu-lien-quan.md`
- `docs/knowledge/bhyt/faq-bhyt.md`
- `docs/knowledge/bhyt/bieugia_bhyt.md`
- `data/mvp/manifest.json` và `data/mvp/seed/knowledge-base.json` — registry hiện tại cần reconcile.

## 5. Execution Boundaries

In scope:

- Knowledge source inventory, source allowlist, source/version approval metadata and seed entity mapping.

Out of scope:

- Duyệt thủ công từng chunk hoặc xây workflow duyệt nhiều bước trong ingestion.
- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-003` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Knowledge source inventory, registration report and seed entity mapping.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Tất cả JSON/manifest và metadata nguồn trong `docs/knowledge` hợp lệ; đường dẫn legacy được thay thế.
- Chỉ tài liệu nghiệp vụ trong source allowlist được đăng ký ingest; `docs/knowledge/spec-normalization-plan.md` và metadata frontmatter không được chunk như nội dung trả lời.
- Bảy tài liệu BHYT bootstrap, gồm biểu giá, được đăng ký `approved_for_pilot`; file bootstrap không có frontmatter nhận metadata mặc định từ source registry.
- Approval áp dụng ở cấp source/version; chunk do WP-008 sinh ra chỉ kế thừa metadata và không cần con người duyệt riêng trong MVP.
- Nội dung bổ sung sau bootstrap mặc định `draft`; Human Lead phải cập nhật source registry trước khi nội dung được index. Workflow duyệt nhiều bước và UI quản trị không thuộc WP-007/WP-008.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-007.
