---
pack_id: WP-403
pack_type: work-package
work_package: WP-403
capability: "PC-03"
artifact_dependencies:
  - INT-02
  - INT-08
  - INT-09
contract_dependencies:
  - "AppointmentBookingAPI"
  - "AppointmentBookingRequest"
  - "AppointmentBookingResponse"
  - "CapabilityResponseEnvelope"
upstream_dependencies:
  - WP-305
  - WP-101
  - WP-104
  - WP-105
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-403

## 1. Purpose

Expose booking execute/resume API cho LangGraph confirmation workflow.

## 2. Required Context

- Layer/module: L4 / M4.3.
- Capability trace: Appointment Booking: thu thập nhiều lượt, xác nhận và tạo lịch pending. 
- Owner pool/size: API / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-403 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/capability-api-contracts.md` — đọc các phần canonical liên quan tới INT-02.
- `docs/artifacts/interface/interaction-sequences.md` — đọc các phần canonical liên quan tới INT-08.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `AppointmentBookingAPI` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentBookingRequest` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentBookingResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `CapabilityResponseEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-03 endpoint and contract tests.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-305` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-101` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-104` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-105` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-03 endpoint and contract tests.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Resume xác minh thread ownership và confirmation token.
- Submit idempotent và chỉ trả pending_staff_review, không trả confirmed giả.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-403.
