---
pack_id: WP-501
pack_type: work-package
work_package: WP-501
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - ARCH-03
  - INT-02
  - INT-09
contract_dependencies:
  - "CapabilityResponseEnvelope"
  - "ClientContextDTO"
  - "ChannelConfigurationDTO"
upstream_dependencies:
  - WP-401
  - WP-402
  - WP-403
  - WP-404
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-501

## 1. Purpose

Tạo client dùng chung cho widget và standalone chat.

## 2. Required Context

- Layer/module: L5 / M5.1.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: FE / L.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-501 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/component-architecture.md` — đọc các phần canonical liên quan tới ARCH-03.
- `docs/artifacts/interface/capability-api-contracts.md` — đọc các phần canonical liên quan tới INT-02.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `CapabilityResponseEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ClientContextDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ChannelConfigurationDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Shared chat client, widget and standalone shells.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-401` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-402` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-403` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-404` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Shared chat client, widget and standalone shells.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Demo được cả hai kênh.
- Auth/session/error/stream handling nhất quán.
- SSE/client hỗ trợ confirmation_required và resume bằng opaque thread/run reference.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-501.
