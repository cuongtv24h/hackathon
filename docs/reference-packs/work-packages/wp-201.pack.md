---
pack_id: WP-201
pack_type: work-package
work_package: WP-201
capability: "PC-01"
artifact_dependencies:
  - ARCH-06
  - INT-06
  - INT-07
contract_dependencies:
  - "search_knowledge_base"
  - "fallback_response"
  - "KnowledgeSearchResponse"
upstream_dependencies:
  - WP-102
  - WP-009
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-201

## 1. Purpose

Hiện thực tool retrieval grounded và fallback theo contract.

## 2. Required Context

- Layer/module: L2 / M2.1.
- Capability trace: Information Assistance: trả lời grounded, citation, fallback và content operations liên quan. 
- Owner pool/size: AI-CORE / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-201 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/tool-map.md` — đọc các phần canonical liên quan tới ARCH-06.
- `docs/artifacts/interface/tool-contracts.md` — đọc các phần canonical liên quan tới INT-06.
- `docs/artifacts/interface/error-contracts.md` — đọc các phần canonical liên quan tới INT-07.

## 4. Required Contracts

- `search_knowledge_base` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `fallback_response` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `KnowledgeSearchResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Knowledge and fallback tool adapters.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-102` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Knowledge and fallback tool adapters.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Tool trả citation/provenance.
- Insufficient/conflict kích hoạt fallback.
- Retrieval bắt buộc lọc `is_active`, effective date và approval status trước khi trả chunk.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-201.
