---
pack_id: WP-005
pack_type: work-package
work_package: WP-005
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - ARCH-02
  - ARCH-08
  - INT-04
  - INT-09
contract_dependencies:
  - "SessionDTO"
  - "KnowledgeChunkDTO"
  - "AppointmentDTO"
  - "EmergencyEventReceiptDTO"
upstream_dependencies:
  - WP-003
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-005

## 1. Purpose

Triển khai schema/migration từ domain và retention contracts đã chốt.

## 2. Required Context

- Layer/module: L0 / M0.0.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: DATA / L.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-005 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/domain-model.md` — đọc các phần canonical liên quan tới ARCH-02.
- `docs/artifacts/architecture/integration-data-flow.md` — đọc các phần canonical liên quan tới ARCH-08.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `SessionDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `KnowledgeChunkDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencyEventReceiptDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Supabase migrations and schema documentation.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-003` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Supabase migrations and schema documentation.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Bao phủ entity MVP và quan hệ canonical.
- Migration có thể áp dụng trên môi trường sạch.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-005.

