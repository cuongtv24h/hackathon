---
pack_id: WP-301
pack_type: work-package
work_package: WP-301
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - ARCH-03
  - ARCH-09
  - INT-05
contract_dependencies:
  - "ReasoningResultDTO"
  - "UnifiedErrorEnvelope"
upstream_dependencies:
  - WP-201
  - WP-202
  - WP-203
  - WP-204
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-301

## 1. Purpose

Cô lập provider LLM/embedding theo kiến trúc đã chốt.

## 2. Required Context

- Layer/module: L3 / M3.1.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: AI-CORE / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-301 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/component-architecture.md` — đọc các phần canonical liên quan tới ARCH-03.
- `docs/artifacts/architecture/deployment-resilience.md` — đọc các phần canonical liên quan tới ARCH-09.
- `docs/artifacts/interface/ai-behavior-contracts.md` — đọc các phần canonical liên quan tới INT-05.

## 4. Required Contracts

- `ReasoningResultDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `UnifiedErrorEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- LLM and embedding provider adapters.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-201` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-202` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-203` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-204` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- LLM and embedding provider adapters.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Không rò rỉ provider vào Capability API.
- Timeout/failure ánh xạ error contract.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-301.
