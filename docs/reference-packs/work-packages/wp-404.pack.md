---
pack_id: WP-404
pack_type: work-package
work_package: WP-404
capability: "PC-04"
artifact_dependencies:
  - INT-02
  - INT-08
  - INT-09
contract_dependencies:
  - "AppointmentStatusAPI"
  - "AppointmentStatusRequest"
  - "AppointmentStatusResponse"
  - "CapabilityResponseEnvelope"
upstream_dependencies:
  - WP-306
  - WP-101
  - WP-104
  - WP-105
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-404

## 1. Purpose

Expose authenticated appointment/request status profile qua shared agent.

## 2. Required Context

- Layer/module: L4 / M4.4.
- Capability trace: Appointment Status: tra cứu trạng thái bằng tham chiếu tối thiểu. 
- Owner pool/size: API / S.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-404 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/capability-api-contracts.md` — đọc các phần canonical liên quan tới INT-02.
- `docs/artifacts/interface/interaction-sequences.md` — đọc các phần canonical liên quan tới INT-08.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `AppointmentStatusAPI` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentStatusRequest` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentStatusResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `CapabilityResponseEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-04 endpoint and contract tests.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-306` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-101` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-104` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-105` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-04 endpoint and contract tests.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Lookup không yêu cầu dữ liệu thừa.
- Not-found/error đúng unified contract.
- Auth failure và cross-patient lookup không được phân biệt theo cách làm lộ dữ liệu.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-404.
