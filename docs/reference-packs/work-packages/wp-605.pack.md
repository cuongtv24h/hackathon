---
pack_id: WP-605
pack_type: work-package
work_package: WP-605
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - INT-03
  - INT-08
  - INT-09
contract_dependencies:
  - "ContentConflictDTO"
  - "AnalyticsSummaryDTO"
  - "ConversationHistoryPageDTO"
upstream_dependencies:
  - WP-505
  - WP-506
  - WP-601
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-605

## 1. Purpose

Kiểm thử content workflow, conflict, analytics và full-access demo role.

## 2. Required Context

- Layer/module: L6 / M6.5.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: QA / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-605 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/foundation-api-contracts.md` — đọc các phần canonical liên quan tới INT-03.
- `docs/artifacts/interface/interaction-sequences.md` — đọc các phần canonical liên quan tới INT-08.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `ContentConflictDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AnalyticsSummaryDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ConversationHistoryPageDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Admin integration and E2E tests.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-505` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-506` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-601` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Admin integration and E2E tests.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Conflict notification hiển thị trên dashboard.
- Analytics không lộ PII và filter đúng.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-605.

