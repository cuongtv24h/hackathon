---
pack_id: WP-402
pack_type: work-package
work_package: WP-402
capability: "PC-02"
artifact_dependencies:
  - INT-02
  - INT-08
  - INT-09
contract_dependencies:
  - "EmergencySafetyAPI"
  - "EmergencySafetyRequest"
  - "EmergencySafetyResponse"
  - "CapabilityResponseEnvelope"
upstream_dependencies:
  - WP-304
  - WP-101
  - WP-103
  - WP-105
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-402

## 1. Purpose

Expose deterministic emergency và restricted safety-agent paths qua cùng response contract.

## 2. Required Context

- Layer/module: L4 / M4.2.
- Capability trace: Emergency Safety: pre-filter deterministic, protocol an toàn và Level-1 contact handoff. 
- Owner pool/size: API / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-402 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/capability-api-contracts.md` — đọc các phần canonical liên quan tới INT-02.
- `docs/artifacts/interface/interaction-sequences.md` — đọc các phần canonical liên quan tới INT-08.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `EmergencySafetyAPI` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencySafetyRequest` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencySafetyResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `CapabilityResponseEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-02 endpoint and contract tests.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-304` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-101` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-103` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-105` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-02 endpoint and contract tests.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Critical response không phụ thuộc LLM availability.
- Protocol/event receipt đúng contract.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-402.
