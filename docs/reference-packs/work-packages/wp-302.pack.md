---
pack_id: WP-302
pack_type: work-package
work_package: WP-302
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - ARCH-05
  - ARCH-07
  - INT-05
  - INT-07
contract_dependencies:
  - "AgentDecisionDTO"
  - "ToolPolicyDecisionDTO"
  - "ObservationResultDTO"
  - "ConversationResultDTO"
  - "ExplainabilityResultDTO"
  - "GroundingFallbackBehavior"
upstream_dependencies:
  - WP-301
  - WP-201
  - WP-202
  - WP-203
  - WP-204
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-302

## 1. Purpose

Xây LangGraph bounded agent cho context, dynamic tool visibility, policy gates, checkpoint/interrupt, guardrail, grounding, explainability và fallback; không có intent router bắt buộc.

## 2. Required Context

- Layer/module: L3 / M3.2.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: AI-CORE / L.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-302 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/ai-capability-mapping.md` — đọc các phần canonical liên quan tới ARCH-05.
- `docs/artifacts/architecture/context-design.md` — đọc các phần canonical liên quan tới ARCH-07.
- `docs/artifacts/interface/ai-behavior-contracts.md` — đọc các phần canonical liên quan tới INT-05.
- `docs/artifacts/interface/error-contracts.md` — đọc các phần canonical liên quan tới INT-07.

## 4. Required Contracts

- `AgentDecisionDTO` — next action provider-neutral; không chứa hoặc lưu chain-of-thought.
- `ToolPolicyDecisionDTO` — allow/deny/confirmation do backend quyết định, không do model tự cấp quyền.
- `ObservationResultDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ConversationResultDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ExplainabilityResultDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `GroundingFallbackBehavior` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- LangGraph state graph and bounded native tool-calling loop.
- Dynamic tool provider, execution-time policy gate and controlled tool executor.
- PostgreSQL checkpointer integration contract and in-memory test checkpointer.
- Confirmation interrupt/resume, execution budget and grounding verification.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-301` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-201` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-202` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-203` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-204` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- LangGraph bounded agent, persistence, policy and guardrail core.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Emergency prefilter chạy trước reasoning.
- Output có grounding/explainability và không lộ chain-of-thought.
- Không có intent-router node; model tự chọn tool từ visible subset.
- Authorization/workflow policy được kiểm tra lại trước execution.
- Checkpoint, confirmation interrupt/resume, duplicate-call prevention và budget exhaustion được kiểm thử.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-302.
