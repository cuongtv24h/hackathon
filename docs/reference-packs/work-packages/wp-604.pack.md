---
pack_id: WP-604
pack_type: work-package
work_package: WP-604
capability: "PC-03,PC-04"
artifact_dependencies:
  - ARCH-04
  - INT-08
contract_dependencies:
  - "AppointmentBookingAPI"
  - "AppointmentStatusAPI"
  - "AppointmentDTO"
upstream_dependencies:
  - WP-403
  - WP-404
  - WP-503
  - WP-601
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-604

## 1. Purpose

Kiểm thử booking/status với Mock HIS và idempotency.

## 2. Required Context

- Layer/module: L6 / M6.4.
- Capability trace: Appointment Booking và Appointment Status dùng chung Appointment Foundation/Mock HIS. 
- Owner pool/size: QA / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-604 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/business-sequences.md` — đọc các phần canonical liên quan tới ARCH-04.
- `docs/artifacts/interface/interaction-sequences.md` — đọc các phần canonical liên quan tới INT-08.

## 4. Required Contracts

- `AppointmentBookingAPI` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentStatusAPI` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-03/PC-04 integration and E2E tests.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-403` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-404` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-503` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-601` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-03/PC-04 integration and E2E tests.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Create luôn pending và không duplicate.
- Lookup found/not-found/error pass.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-604.

