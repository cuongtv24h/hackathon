---
pack_id: WP-505
pack_type: work-package
work_package: WP-505
capability: "PC-01"
artifact_dependencies:
  - ARCH-03
  - INT-03
  - INT-04
contract_dependencies:
  - "ContentDraftDTO"
  - "ContentConflictPageDTO"
  - "ContentConflictResolveRequest"
upstream_dependencies:
  - WP-501
  - WP-102
  - WP-101
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-505

## 1. Purpose

Cung cấp content workflow và cảnh báo conflict trên dashboard.

## 2. Required Context

- Layer/module: L5 / M5.5.
- Capability trace: Information Assistance: trả lời grounded, citation, fallback và content operations liên quan. 
- Owner pool/size: FE / L.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-505 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/component-architecture.md` — đọc các phần canonical liên quan tới ARCH-03.
- `docs/artifacts/interface/foundation-api-contracts.md` — đọc các phần canonical liên quan tới INT-03.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.

## 4. Required Contracts

- `ContentDraftDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ContentConflictPageDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ContentConflictResolveRequest` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Admin content management views.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-501` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-102` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-101` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Admin content management views.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Tài khoản demo full quyền xử lý được workflow.
- Conflict hiển thị và resolve có audit.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-505.

