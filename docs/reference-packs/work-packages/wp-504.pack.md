---
pack_id: WP-504
pack_type: work-package
work_package: WP-504
capability: "PC-02"
artifact_dependencies:
  - ARCH-09
  - INT-02
  - INT-04
contract_dependencies:
  - "EmergencySafetyResponse"
  - "SuggestedActionDTO"
upstream_dependencies:
  - WP-501
  - WP-402
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-504

## 1. Purpose

Hiển thị safety banner và Level-1 contact handoff.

## 2. Required Context

- Layer/module: L5 / M5.4.
- Capability trace: Emergency Safety: pre-filter deterministic, protocol an toàn và Level-1 contact handoff. 
- Owner pool/size: FE / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-504 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/deployment-resilience.md` — đọc các phần canonical liên quan tới ARCH-09.
- `docs/artifacts/interface/capability-api-contracts.md` — đọc các phần canonical liên quan tới INT-02.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.

## 4. Required Contracts

- `EmergencySafetyResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `SuggestedActionDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Emergency UI and contact actions.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-501` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-402` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Emergency UI and contact actions.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Banner ưu tiên cao, không bị streaming che.
- Hotline/address/URL mock được gắn cảnh báo cấu hình.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-504.

