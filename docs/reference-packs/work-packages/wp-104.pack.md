---
pack_id: WP-104
pack_type: work-package
work_package: WP-104
capability: "PC-03,PC-04"
artifact_dependencies:
  - ARCH-03
  - ARCH-08
  - INT-03
  - INT-04
contract_dependencies:
  - "FND-APT-01 ListSpecialties"
  - "FND-APT-02 ListDoctors"
  - "FND-APT-03 ListAvailableSlots"
  - "FND-APT-04 CreateAppointment"
  - "FND-APT-05 GetAppointment"
  - "AppointmentDTO"
upstream_dependencies:
  - WP-008
  - WP-009
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-104

## 1. Purpose

Cung cấp khoa, bác sĩ, slot, tạo và tra cứu lịch qua Mock HIS.

## 2. Required Context

- Layer/module: L1 / M1.4.
- Capability trace: Appointment Booking và Appointment Status dùng chung Appointment Foundation/Mock HIS. 
- Owner pool/size: API / L.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-104 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/component-architecture.md` — đọc các phần canonical liên quan tới ARCH-03.
- `docs/artifacts/architecture/integration-data-flow.md` — đọc các phần canonical liên quan tới ARCH-08.
- `docs/artifacts/interface/foundation-api-contracts.md` — đọc các phần canonical liên quan tới INT-03.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.

## 4. Required Contracts

- `FND-APT-01 ListSpecialties` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-APT-02 ListDoctors` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-APT-03 ListAvailableSlots` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-APT-04 CreateAppointment` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-APT-05 GetAppointment` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Appointment service and Mock HIS adapter.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-008` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Appointment service and Mock HIS adapter.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Lịch mới mặc định pending.
- Create idempotent; lookup dùng tham chiếu tối thiểu.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-104.

