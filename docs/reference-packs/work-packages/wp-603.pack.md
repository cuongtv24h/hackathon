---
pack_id: WP-603
pack_type: work-package
work_package: WP-603
capability: "PC-02"
artifact_dependencies:
  - ARCH-09
  - INT-08
contract_dependencies:
  - "EmergencySafetyAPI"
  - "PreFilterResultDTO"
  - "EmergencyProtocolDTO"
upstream_dependencies:
  - WP-402
  - WP-504
  - WP-601
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-603

## 1. Purpose

Kiểm thử keyword, negative context, LLM outage và handoff.

## 2. Required Context

- Layer/module: L6 / M6.3.
- Capability trace: Emergency Safety: pre-filter deterministic, protocol an toàn và Level-1 contact handoff. 
- Owner pool/size: QA / L.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-603 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/deployment-resilience.md` — đọc các phần canonical liên quan tới ARCH-09.
- `docs/artifacts/interface/interaction-sequences.md` — đọc các phần canonical liên quan tới INT-08.

## 4. Required Contracts

- `EmergencySafetyAPI` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `PreFilterResultDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencyProtocolDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-02 safety, fault and E2E tests.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-402` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-504` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-601` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-02 safety, fault and E2E tests.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Critical path pass khi LLM lỗi.
- Không có chẩn đoán; mock warning được kiểm tra.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-603.

