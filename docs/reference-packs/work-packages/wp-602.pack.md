---
pack_id: WP-602
pack_type: work-package
work_package: WP-602
capability: "PC-01"
artifact_dependencies:
  - ARCH-04
  - INT-08
contract_dependencies:
  - "InformationAssistanceAPI"
  - "KnowledgeSearchResponse"
  - "GroundingFallbackBehavior"
upstream_dependencies:
  - WP-401
  - WP-502
  - WP-601
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-602

## 1. Purpose

Kiểm thử PC-01 từ hai kênh đến retrieval/citation.

## 2. Required Context

- Layer/module: L6 / M6.2.
- Capability trace: Information Assistance: trả lời grounded, citation, fallback và content operations liên quan. 
- Owner pool/size: QA / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-602 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/business-sequences.md` — đọc các phần canonical liên quan tới ARCH-04.
- `docs/artifacts/interface/interaction-sequences.md` — đọc các phần canonical liên quan tới INT-08.

## 4. Required Contracts

- `InformationAssistanceAPI` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `KnowledgeSearchResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `GroundingFallbackBehavior` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-01 integration and E2E tests.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-401` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-502` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-601` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-01 integration and E2E tests.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- BHYT approved path, retrieval-insufficient, out-of-scope và conflict cases đều pass.
- Citation truy vết đúng approved source.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-602.
