---
pack_id: WP-203
pack_type: work-package
work_package: WP-203
capability: "PC-03,PC-04"
artifact_dependencies:
  - ARCH-06
  - INT-06
  - INT-07
contract_dependencies:
  - "get_specialty_list"
  - "get_doctor_list"
  - "get_available_slots"
  - "create_appointment"
  - "lookup_appointment"
upstream_dependencies:
  - WP-104
  - WP-009
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-203

## 1. Purpose

Bọc Foundation APIs thành tool appointment provider-neutral.

## 2. Required Context

- Layer/module: L2 / M2.3.
- Capability trace: Appointment Booking và Appointment Status dùng chung Appointment Foundation/Mock HIS. 
- Owner pool/size: API / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-203 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/tool-map.md` — đọc các phần canonical liên quan tới ARCH-06.
- `docs/artifacts/interface/tool-contracts.md` — đọc các phần canonical liên quan tới INT-06.
- `docs/artifacts/interface/error-contracts.md` — đọc các phần canonical liên quan tới INT-07.

## 4. Required Contracts

- `get_specialty_list` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `get_doctor_list` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `get_available_slots` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `create_appointment` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `lookup_appointment` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Appointment tool adapters.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-104` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Appointment tool adapters.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Input/output đúng tool contract.
- Timeout/retry/idempotency đúng chính sách.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-203.

