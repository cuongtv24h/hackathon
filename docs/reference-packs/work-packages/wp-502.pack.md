---
pack_id: WP-502
pack_type: work-package
work_package: WP-502
capability: "PC-01"
artifact_dependencies:
  - INT-02
  - INT-04
  - INT-09
contract_dependencies:
  - "InformationAssistanceResponse"
  - "CitationDTO"
  - "SuggestedActionDTO"
  - "ExplainabilityDTO"
upstream_dependencies:
  - WP-501
  - WP-401
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-502

## 1. Purpose

Hiển thị câu trả lời grounded, nguồn và next actions.

## 2. Required Context

- Layer/module: L5 / M5.2.
- Capability trace: Information Assistance: trả lời grounded, citation, fallback và content operations liên quan. 
- Owner pool/size: FE / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-502 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/capability-api-contracts.md` — đọc các phần canonical liên quan tới INT-02.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `InformationAssistanceResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `CitationDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `SuggestedActionDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ExplainabilityDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-01 chat UI states.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-501` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-401` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-01 chat UI states.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Citation truy cập được.
- Fallback/conflict không hiển thị như câu trả lời chắc chắn.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-502.

