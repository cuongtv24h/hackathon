---
pack_id: WP-101
pack_type: work-package
work_package: WP-101
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - INT-03
  - INT-04
  - INT-07
contract_dependencies:
  - "FND-SES-01 CreateSession"
  - "FND-SES-02 GetSessionContext"
  - "FND-SES-03 PatchSessionContext"
  - "FND-CFG-01 GetChannels"
  - "FND-CFG-02 GetChatConfiguration"
  - "FND-FBK-01 CreateFeedback"
upstream_dependencies:
  - WP-006
  - WP-009
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-101

## 1. Purpose

Cung cấp session/context/config/feedback không có AI reasoning.

## 2. Required Context

- Layer/module: L1 / M1.1.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: API / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-101 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/foundation-api-contracts.md` — đọc các phần canonical liên quan tới INT-03.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.
- `docs/artifacts/interface/error-contracts.md` — đọc các phần canonical liên quan tới INT-07.

## 4. Required Contracts

- `FND-SES-01 CreateSession` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-SES-02 GetSessionContext` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-SES-03 PatchSessionContext` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-CFG-01 GetChannels` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-CFG-02 GetChatConfiguration` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-FBK-01 CreateFeedback` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Foundation service modules and contract tests.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-006` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Foundation service modules and contract tests.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Foundation API không gọi LLM.
- Session và config đúng retention/channel.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-101.

