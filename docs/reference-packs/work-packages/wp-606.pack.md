---
pack_id: WP-606
pack_type: work-package
work_package: WP-606
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - ARCH-01
  - ARCH-09
  - INT-07
  - INT-09
contract_dependencies:
  - "RetryPolicy"
  - "FallbackPolicy"
  - "UnifiedErrorEnvelope"
upstream_dependencies:
  - WP-602
  - WP-603
  - WP-604
  - WP-605
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-606

## 1. Purpose

Thực hiện release gate cuối cho MVP Pilot trên VPS.

## 2. Required Context

- Layer/module: L6 / M6.6.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: QA / L.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-606 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/solution-constraints.md` — đọc các phần canonical liên quan tới ARCH-01.
- `docs/artifacts/architecture/deployment-resilience.md` — đọc các phần canonical liên quan tới ARCH-09.
- `docs/artifacts/interface/error-contracts.md` — đọc các phần canonical liên quan tới INT-07.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `RetryPolicy` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FallbackPolicy` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `UnifiedErrorEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Release evidence, runbook checks and go/no-go report.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-602` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-603` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-604` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-605` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Release evidence, runbook checks and go/no-go report.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Rate limit/auth/RLS/fallback/performance đạt mức pilot.
- Placeholder contact và emergency mock được chặn hoặc sign-off trước demo thật.
- Chỉ nguồn bootstrap `approved_for_pilot` hoặc content đã được workflow phê duyệt mới answerable.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-606.
