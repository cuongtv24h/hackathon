---
pack_id: WP-009
pack_type: work-package
work_package: WP-009
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - INT-04
  - INT-07
  - INT-09
contract_dependencies:
  - "CapabilityResponseEnvelope"
  - "UnifiedErrorEnvelope"
  - "ClientContextDTO"
  - "ChannelConfigurationDTO"
  - "ChatConfigurationDTO"
upstream_dependencies:
  - WP-003
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-009

## 1. Purpose

Hiện thực hóa shared agent-state, policy, budget, interrupt/resume, response/error và runtime configuration contracts.

## 2. Required Context

- Layer/module: L0 / M0.3.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: API / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-009 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.
- `docs/artifacts/interface/error-contracts.md` — đọc các phần canonical liên quan tới INT-07.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `CapabilityResponseEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `UnifiedErrorEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ClientContextDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ChannelConfigurationDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ChatConfigurationDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Shared contract and configuration libraries.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-003` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Shared contract and configuration libraries.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Validation tuân thủ DTO canonical.
- Response/error/versioning thống nhất.
- Agent state/policy/budget/interrupt contracts không chứa secret, raw PII hoặc chain-of-thought; runtime config mặc định OpenAI và hỗ trợ environment override.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-009.
