---
pack_id: WP-202
pack_type: work-package
work_package: WP-202
capability: "PC-02"
artifact_dependencies:
  - ARCH-06
  - ARCH-09
  - INT-06
contract_dependencies:
  - "trigger_emergency"
  - "PreFilterResultDTO"
  - "EmergencyEventReceiptDTO"
upstream_dependencies:
  - WP-103
  - WP-009
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-202

## 1. Purpose

Hiện thực safety-first high/caution/low gate trước agent và restricted emergency tools.

## 2. Required Context

- Layer/module: L2 / M2.2.
- Capability trace: Emergency Safety: pre-filter deterministic, protocol an toàn và Level-1 contact handoff. 
- Owner pool/size: AI-CORE / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-202 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/tool-map.md` — đọc các phần canonical liên quan tới ARCH-06.
- `docs/artifacts/architecture/deployment-resilience.md` — đọc các phần canonical liên quan tới ARCH-09.
- `docs/artifacts/interface/tool-contracts.md` — đọc các phần canonical liên quan tới INT-06.

## 4. Required Contracts

- `trigger_emergency` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `PreFilterResultDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencyEventReceiptDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Emergency prefilter and tool adapter.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-103` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Emergency prefilter and tool adapter.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Kiểm tra trước LLM.
- Negative context giảm false positive theo test seed.
- High bypass LLM; caution/uncertain chỉ vào restricted safety profile; low mới vào general agent.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-202.
