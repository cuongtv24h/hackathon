---
pack_id: WP-503
pack_type: work-package
work_package: WP-503
capability: "PC-03,PC-04"
artifact_dependencies:
  - INT-02
  - INT-04
  - INT-08
contract_dependencies:
  - "AppointmentBookingResponse"
  - "AppointmentStatusResponse"
  - "BookingFlowStateDTO"
  - "AppointmentDTO"
upstream_dependencies:
  - WP-501
  - WP-403
  - WP-404
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-503

## 1. Purpose

Hiển thị flow đặt lịch và tra cứu trong chat.

## 2. Required Context

- Layer/module: L5 / M5.3.
- Capability trace: Appointment Booking và Appointment Status dùng chung Appointment Foundation/Mock HIS. 
- Owner pool/size: FE / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-503 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/capability-api-contracts.md` — đọc các phần canonical liên quan tới INT-02.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.
- `docs/artifacts/interface/interaction-sequences.md` — đọc các phần canonical liên quan tới INT-08.

## 4. Required Contracts

- `AppointmentBookingResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentStatusResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `BookingFlowStateDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Appointment chat UI states.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-501` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-403` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-404` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Appointment chat UI states.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Có bước confirm rõ ràng.
- Pending/not-found/error hiển thị đúng.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-503.

