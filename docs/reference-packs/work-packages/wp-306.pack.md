---
pack_id: WP-306
pack_type: work-package
work_package: WP-306
capability: "PC-04"
artifact_dependencies:
  - ARCH-04
  - ARCH-05
  - INT-05
contract_dependencies:
  - "AppointmentStatusRequest"
  - "AppointmentStatusResponse"
  - "AppointmentDTO"
upstream_dependencies:
  - WP-302
  - WP-203
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-306

## 1. Purpose

Điều phối tra cứu trạng thái và next action an toàn.

## 2. Required Context

- Layer/module: L3 / M3.6.
- Capability trace: Appointment Status: tra cứu trạng thái bằng tham chiếu tối thiểu. 
- Owner pool/size: AI-CORE / S.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-306 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/business-sequences.md` — đọc các phần canonical liên quan tới ARCH-04.
- `docs/artifacts/architecture/ai-capability-mapping.md` — đọc các phần canonical liên quan tới ARCH-05.
- `docs/artifacts/interface/ai-behavior-contracts.md` — đọc các phần canonical liên quan tới INT-05.

## 4. Required Contracts

- `AppointmentStatusRequest` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentStatusResponse` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AppointmentDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- PC-04 orchestration pipeline.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-302` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-203` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- PC-04 orchestration pipeline.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Không suy diễn trạng thái.
- Không tìm thấy trả fallback an toàn.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-306.

