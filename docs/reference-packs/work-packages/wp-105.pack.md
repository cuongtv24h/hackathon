---
pack_id: WP-105
pack_type: work-package
work_package: WP-105
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - ARCH-08
  - INT-03
  - INT-04
contract_dependencies:
  - "FND-HIS-01 GetConversationHistory"
  - "FND-ANA-01 GetAnalyticsSummary"
  - "ConversationHistoryPageDTO"
  - "AnalyticsSummaryDTO"
upstream_dependencies:
  - WP-006
  - WP-009
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-105

## 1. Purpose

Lưu hội thoại ẩn danh, analytics và immutable agent/tool/policy/approval audit history.

## 2. Required Context

- Layer/module: L1 / M1.5.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: API / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-105 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/integration-data-flow.md` — đọc các phần canonical liên quan tới ARCH-08.
- `docs/artifacts/interface/foundation-api-contracts.md` — đọc các phần canonical liên quan tới INT-03.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.

## 4. Required Contracts

- `FND-HIS-01 GetConversationHistory` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-ANA-01 GetAnalyticsSummary` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `ConversationHistoryPageDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `AnalyticsSummaryDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Logging, analytics and audit services.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-006` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Logging, analytics and audit services.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- PII không được ghi vào analytics.
- Retention và audit integrity được kiểm thử.
- Audit persistence tách khỏi LangGraph checkpoint và không lưu chain-of-thought.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-105.
