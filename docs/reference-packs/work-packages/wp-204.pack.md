---
pack_id: WP-204
pack_type: work-package
work_package: WP-204
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - ARCH-06
  - INT-06
  - INT-07
contract_dependencies:
  - "detect_pii"
  - "log_conversation"
upstream_dependencies:
  - WP-105
  - WP-009
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-204

## 1. Purpose

Ẩn danh PII và audit agent/tool/policy/approval transitions mà không lưu chain-of-thought.

## 2. Required Context

- Layer/module: L2 / M2.4.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: AI-CORE / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-204 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/tool-map.md` — đọc các phần canonical liên quan tới ARCH-06.
- `docs/artifacts/interface/tool-contracts.md` — đọc các phần canonical liên quan tới INT-06.
- `docs/artifacts/interface/error-contracts.md` — đọc các phần canonical liên quan tới INT-07.

## 4. Required Contracts

- `detect_pii` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `log_conversation` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Cross-cutting privacy tool adapters.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-105` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Cross-cutting privacy tool adapters.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- PII category được phát hiện/che.
- Logging failure không làm lộ dữ liệu hoặc phá response.
- Audit ghi tool, validated-argument digest, authorization outcome, result status và approval decision.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-204.
