---
pack_id: WP-103
pack_type: work-package
work_package: WP-103
capability: "PC-02"
artifact_dependencies:
  - ARCH-01
  - ARCH-09
  - INT-03
  - INT-04
contract_dependencies:
  - "FND-EMG-01 GetEmergencyProtocol"
  - "FND-EMG-02 GetEmergencyKeywordSet"
  - "FND-EMG-03 CreateEmergencyEvent"
  - "EmergencyProtocolDTO"
upstream_dependencies:
  - WP-008
  - WP-009
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-103

## 1. Purpose

Phục vụ keyword/protocol local và ghi emergency event.

## 2. Required Context

- Layer/module: L1 / M1.3.
- Capability trace: Emergency Safety: pre-filter deterministic, protocol an toàn và Level-1 contact handoff. 
- Owner pool/size: API / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-103 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/architecture/solution-constraints.md` — đọc các phần canonical liên quan tới ARCH-01.
- `docs/artifacts/architecture/deployment-resilience.md` — đọc các phần canonical liên quan tới ARCH-09.
- `docs/artifacts/interface/foundation-api-contracts.md` — đọc các phần canonical liên quan tới INT-03.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.

## 4. Required Contracts

- `FND-EMG-01 GetEmergencyProtocol` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-EMG-02 GetEmergencyKeywordSet` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FND-EMG-03 CreateEmergencyEvent` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `EmergencyProtocolDTO` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Emergency protocol loader and event service.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-008` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Emergency protocol loader and event service.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Đường critical không phụ thuộc LLM.
- Mock clinical warning luôn được bảo toàn.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-103.

