---
pack_id: WP-601
pack_type: work-package
work_package: WP-601
capability: "ALL_CAPABILITIES"
artifact_dependencies:
  - INT-03
  - INT-04
  - INT-07
  - INT-09
contract_dependencies:
  - "UnifiedErrorEnvelope"
  - "RetryPolicy"
  - "FallbackPolicy"
upstream_dependencies:
  - WP-008
  - WP-009
  - WP-101
  - WP-102
  - WP-103
  - WP-104
  - WP-105
target_audience:
  - builder
  - reviewer
---

# Work Package Pack — WP-601

## 1. Purpose

Kiểm tra schema, seed, DTO và Foundation API trước E2E.

## 2. Required Context

- Layer/module: L6 / M6.1.
- Capability trace: Cross-cutting foundation cho PC-01, PC-02, PC-03 và PC-04. 
- Owner pool/size: QA / M.
- Kế hoạch canonical: `docs/5.ai-engineering-planning.md` và entry WP-601 trong `docs/spec-registry/work-package-map.yaml`.

## 3. Required Artifacts

- `docs/artifacts/interface/foundation-api-contracts.md` — đọc các phần canonical liên quan tới INT-03.
- `docs/artifacts/interface/data-contracts.md` — đọc các phần canonical liên quan tới INT-04.
- `docs/artifacts/interface/error-contracts.md` — đọc các phần canonical liên quan tới INT-07.
- `docs/artifacts/interface/interface-guidelines.md` — đọc các phần canonical liên quan tới INT-09.

## 4. Required Contracts

- `UnifiedErrorEnvelope` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `RetryPolicy` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.
- `FallbackPolicy` — tuân thủ nguyên trạng định nghĩa trong artifact ở trên; không đổi field, semantics hoặc error behavior.

## 5. Execution Boundaries

In scope:

- Automated contract and data validation suites.

Out of scope:

- Thiết kế lại architecture, API, DTO, tool hoặc error contract.
- Thực hiện work package downstream chưa đủ dependency.
- Ghi secret, production assumption hoặc dữ liệu bệnh nhân thật vào repository.

## 6. Upstream Dependencies

- `WP-008` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-009` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-101` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-102` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-103` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-104` phải có evidence đạt acceptance criteria trước khi bắt đầu.
- `WP-105` phải có evidence đạt acceptance criteria trước khi bắt đầu.

## 7. Expected Outputs

- Automated contract and data validation suites.

Output file cụ thể chỉ được chốt ở Phase 6 theo component ownership; pack này không tự mở rộng path.

## 8. Validation Expectations

- Toàn bộ seed/DTO contract pass.
- Foundation APIs chứng minh không có AI reasoning.
- RAG corpus validation loại metadata/non-content files và phát hiện source path, approval hoặc version không nhất quán.
- Reviewer kiểm tra traceability tới artifacts/contracts và không có scope drift.
- Mọi automated check liên quan phải có kết quả tái lập được và evidence gắn với WP-601.
