---
pack_id: WP-500
pack_type: work-package
work_package: WP-500
capability: ALL_CAPABILITIES
artifact_dependencies: [ARCH-03, INT-09]
contract_dependencies: []
upstream_dependencies: [WP-009]
target_audience: [builder, reviewer]
---

# Work Package Pack — WP-500

## Purpose

Create independent Vite/React/TypeScript chat and admin application scaffolds with a frontend test harness.

## Required Artifacts

- `docs/5-6-runtime-correction.md`
- `docs/spec-registry/runtime-test-policy.yaml`
- `docs/spec-registry/task-to-file-contract-map.yaml`

## Execution Boundaries

- In scope: contracted Vite/TypeScript entry/configuration files and frontend test setup.
- Out of scope: feature UI, backend API implementation, Python files, secrets and database changes.
- Do not create `__init__.py` or any `.py` inside either frontend application.

## Validation

- `npm run test` in both frontend applications.
- `npm run build` in both frontend applications.
